import { Inter, Lusitana,Averia_Serif_Libre } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });
const lusitana = Lusitana({weight:'400', subsets:['latin']});
const averiaSerif = Averia_Serif_Libre({weight:'400', subsets:['latin']});
export {inter, lusitana, averiaSerif};
